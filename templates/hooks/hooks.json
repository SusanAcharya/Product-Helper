{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": ".cursor/hooks/product-helper-stop.js --session-start"
      }
    ],
    "stop": [
      {
        "type": "prompt",
        "prompt": "If this turn finished a feature, you manage TaskTrack: re-read .product-helper/GUARDRAILS.md, update product.json, PRODUCT.md, TaskTrack, and TIMELINE.md. Feature cards only. Do not ask the human to move cards. End with: run `npx product-helper open`. If the workspace is missing, run `npx product-helper init`. If docs already match, still remind them how to open TaskTrack.",
        "loop_limit": 1
      }
    ]
  }
}
