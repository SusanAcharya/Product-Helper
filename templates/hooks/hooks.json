{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": ".cursor/hooks/product-helper-stop.js --session-start"
      }
    ],
    "stop": [
      {
        "type": "prompt",
        "prompt": "If this turn finished a feature (not a small bug fix), follow the product-helper skill: re-read .product-helper/GUARDRAILS.md, then update product.json, PRODUCT.md, TaskTrack, and TIMELINE.md so they match reality. Feature cards only. The human is the PM — do not invent completed work. If Product-Helper is not initialized, propose `npx product-helper init`. If docs already match, do nothing.",
        "loop_limit": 1
      }
    ]
  }
}
