# Examples

`demo/` is a tiny fictional product (**Lumen Desk**) after `product-helper init`.

```bash
node ../bin/product-helper.js serve --dir demo
```

Then open http://127.0.0.1:4173/ — or open `demo/.product-helper/board/index.html` directly.
